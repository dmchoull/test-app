<%= content_for :actions do %>
    <% unless @profile.new_record? %>
        <%= link_to 'Delete', [current_user, @profile], method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger' %>
    <% end %>
<% end %>

<%= render partial: 'form' %>
